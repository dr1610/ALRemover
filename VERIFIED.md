# 公開ベータの検証記録

検証日：2026-09-14。対象：**ALRemover v1.3.0-beta.1**。

## 新規環境の範囲

同じWindows PC上で、普段使っているNeoとは独立した新規クローン、システムのsite-packagesを使わない新しいPython仮想環境、空のモデル保存先、専用の空のHugging Faceキャッシュを用意した。既存のNeo設定・拡張・モデルはコピーしていない。Python本体とpipの配布パッケージキャッシュは利用した。OS自体を再インストールした検証ではない。

| 項目 | 実測環境 |
|---|---|
| OS / GPU | Windows / RTX 5070 12GB |
| Forge Neo | neo 2.29、`774a6c909952aaf5613d1e33ffda885b5a6532b2` |
| Python | 3.13.12 |
| PyTorch / torchvision | 2.13.0+cu130 / 0.28.0+cu130 |
| Gradio / Transformers | 4.40.0 / 4.57.6 |
| timm / kornia | 1.0.29 / 0.6.12 |
| Pillow / NumPy | 12.3.0 / 2.3.5 |
| Hugging Face Hub | 0.36.2 |

Forge Neoの通常の環境準備を `launch.py --exit` で実行後、拡張を配置して `launch.py --ui-debug-mode --port 7869 --api` で起動した。切り抜きの検証には生成用チェックポイントを導入していない。モデル・設定・出力先は検証用の新規クローン側に分離した。

## 成功した確認

- 単体テスト19件：色とアルファの保持、局所修正、人物所属、取り消し、保存・再開。
- 空の保存先から固定リビジョンのBEN2を自動取得し、実際に推論。取得した重みは380,577,976バイト。
- 初回の切り抜き応答は約21.4秒（ダウンロード・モデル準備を含む）。二枚目は約1.46秒。各一回の参考値で、速度保証や厳密なベンチマークではない。
- 二枚の896×1152の一人画像で、切り抜き・キャラクター／背景／マスク保存・緑と赤の修正・修正のリセット。
- 元RGBが全画素一致。キャラクターと背景のアルファの和が入力アルファと一致。
- 修正していない画素が変わらず、修正線をすべて除くと最初のマスクに戻ること。
- 表示対象と保存対象を別に選べること。方式・修正の有無がPNGに記録されること。
- img2imgのScriptで入力を再生成せず切り抜き、人物と背景の二出力、元のseedとRGBを保持すること。
- txt2img・img2imgのScript一覧にALRemoverが登録され、主な三つの方式選択欄の初期値がBEN2であること。
- 二枚のBEN2マスクが、以前の開発環境の比較結果と全画素一致（最大アルファ差0）。
- 拡張を導入しても、表に記載したホストの主要ライブラリのバージョンが変わらないこと。
- BEN2のみを使った際、ToonOut・SAM2・GroundingDINOの重みが取得されないこと。
- ブラウザーから画像読込→切り抜き→赤／緑のブラシ描画→反映→PNG保存まで操作。赤で前景1,048画素を消し、緑で1,550画素を復元できた。色の切り替え後も修正線を保持し、保存画像の元RGBとアルファの分配を確認した。
- `HF_HUB_OFFLINE=1` で再起動後、取得済みBEN2を使った切り抜きが成功すること（Hugging Face Hubをオフラインにした確認で、全ネットワークを遮断した試験ではない）。

BEN2重みのSHA256：`ea8b7907176a09667c86343dc7d00de6a6d871076cb90bb5f753618fd6fb3ebb`。リビジョンとライセンスは [THIRD_PARTY.md](THIRD_PARTY.md) を参照。

## 新規導入で見つけた修正

新規Neoで設定済みの一時画像フォルダーがまだ存在せず、初回の切り抜き結果表示が失敗した。ALRemoverの画面構築時にそのフォルダーを作成する修正を加え、再び空のモデル保存先・空のHugging Faceキャッシュから一連の処理が成功することを確認した。ホストの設定やコードは変更していない。

Gradio 4.40のブラシパレットで選んだ色が描画時に既定色へ戻る挙動があったため、画面上の「緑：残す」「赤：消す」でブラシの既定色を明示的に更新する方式にした。

## 品質評価の限界

これらは操作・保存・再現性の確認で、切り抜きの正解率ではない。正解マスクを付けた大規模データセットによるIoU等の評価はしていない。腕や白い部分の半透明化、髪の欠け、背景の線の消し残しは起こり得る。

今回の新規環境では、BEN2を使う一人用の流れを重点的に検証した。txt2imgの実際の再生成、ToonOut・順送り・逆送り、多人数のモデル取得と推論はこの新規環境では再検証していない。開発環境での動作記録はあるが、新規環境の一人用と同じ保証範囲には含めない。

多人数の所属は接触した腕・手などで誤ることがあり、実験的機能として扱う。RT-DETRと枠のドラッグ編集は未搭載。別OS・別GPU・A1111・旧Forge・reForgeも未検証。

再現用の操作テストは [tests/verify_single_person.py](tests/verify_single_person.py)。個人の画像・生成情報・ローカルパスを含む生ログとテスト画像は公開物に含めない。
